<div class="da-slide">
	<h2>${title}</h2>
	<p class="slider-text">${text}</p>
	<g:link controller="navigation" action="${action}" class="da-link">Read more</g:link>
	<div class="da-img"><r:img dir="${dir}" file="${file}" alt="${alt}" height="${height}" width="${width}" class="${clazz}" /></div>
</div>